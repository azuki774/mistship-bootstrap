# Repository Instructions

- `docs/agents/` には AI エージェント向けの資料があるため、作業前に確認する。
- `scripts/ops/**` は閲覧・編集のみ可。実行は禁止。
- `make`、`just`、`npm run` などを通じて間接的に `scripts/ops/**` を実行することも禁止。
- `scripts/ops/**` を実行する可能性がある場合は、必ずユーザに確認する。
